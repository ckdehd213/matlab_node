<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("basehead.ejs") %>
    <style>
        .main-img {
            background-image: url("/static/img/main2.jpg");
            background-size: cover; 
            background-position: center center; 
            background-repeat: no-repeat;
        }

        .login-box {
            width: 600px;
            background-color: #25262A;
            padding: 18px 30px;
            border-radius: 20px;
            color: #fff;
        }
        .login-form-box {
            position: relative;
            display: block;
            width: 100%;
            background-color: #2F3034;
            border-radius: 20px;
            padding: 35px 40px;
            color: #fff;
        }
        .fr_input {
            display: block;
            width: 100%;
            height: 46px;
            padding: 0 18px;
            background-color: #CCCCCC;
            box-sizing: border-box;
            border: 0;
            border-radius: 15px;
        }
        .btn-login{
            position: absolute;
            right: 40px;
            top: 35px;
            display: block;
            width: 117px;
            height: 110px;
            background-color: #1365b9;
            color: #fff;
            border-radius: 20px;
            border: none;
        }

        dl {
            display: grid;
            grid-template-columns: 95px 1fr;
            padding-right: 130px;
            box-sizing: border-box;
            margin-bottom: 18px;
            /* flex-wrap: wrap;
            align-items: center;
            margin: 10px 0; */
        }
        dt {
            display: flex;
            color: #fff;
            align-items: center;
            /* flex-basis: 20%;
            text-align: left; */
        }
        dd {
            margin-bottom: 0;
        }
        
        html, body, #root {
            display: flex;
            flex-direction: column;
            width: 100vw;
            height: 100vh;
            overflow: hidden;
            margin: 0;
            padding: 0;
            background-color: rgba(243, 242, 246, 1);
          }

          .flex-1 {
            flex: 1;
          }
        
    </style>
</head>
<body>
<div class="main-img d-flex flex-row flex-1 justify-content-center align-items-center text-center">
    <form action="/main" method="get" class="d-flex flex-column align-items-start">
        <div class="login-box">
            <h5 class="m-3 fw-bold">MATLAB 데이터 전송 테스트</h5>
            <div class="login-form-box">
                    <dl>
                        <dt class="fw-bolder">아이디</dt>
                        <dd>
                            <input class="fr_input" name="uname">
                        </dd>
                    </dl>
                    <dl>
                        <dt class="fw-bolder">비밀번호</dt>
                        <dd>
                            <input class="fr_input" type="password" name="pwd">
                        </dd>
                    </dl>
                    <button type="submit" class="btn-login fw-bolder">로그인</button>
            </div>
            
            <div class="mt-3 fw-bolder">
                
            </div>
        </div>
    </form>
</div>
<script>
document.addEventListener("DOMContentLoaded", () => {
    document.querySelector("input[name=uname]").focus();
});
</script>
</body>
</html>
